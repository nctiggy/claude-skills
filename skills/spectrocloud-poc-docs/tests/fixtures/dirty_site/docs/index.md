# Dirty fixture — every line below should be flagged

Grab the key with `op read op://somevault/some-item/password` first.

Then ssh to the lab box at 172.30.99.7 and run the build.

Power control: wsman --endpoint "https://admin:hunter2secret@10.0.0.9:16993"

```
echo this fence has no language
```

## Step 2 — Out of order

Steps that go backwards:

## Step 1 — Duplicate territory

## Step 1 — Duplicated on purpose

An internal-looking API key literal:

```bash
curl -H "ApiKey: aGVsbG8tdGhpcy1sb29rcy1yZWFsLTAwMDAwMDAw" https://api.spectrocloud.com/v1/projects
```

A hostname the denylist file should catch: buildbox.lab.example

<!-- check-docs:allow — placeholder example, intentionally suppressed -->
This 192.168.1.50 mention is allowed by the marker above.
