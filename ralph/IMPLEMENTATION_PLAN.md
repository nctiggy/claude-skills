# Ralph harness scaffold — no milestone planned yet

`loop/ralph-onboard.sh` (in `craig-ai-tooling/ai-lawnmower`) installed
`ralph/loop.sh` and this scaffold so the repo no longer kills a Ralph Job by
lacking the harness the pod expects to find and run.

This file carries no plan yet. Everything below is written by a planning pass
— `loop/ralph-prepare.sh <task-id> --go` in ai-lawnmower, or the same shape
written by hand — before this repo is dispatched to the cluster.

## Files touched

```allowlist
ralph/loop.sh
ralph/PROMPT.md
ralph/PROGRESS.md
ralph/IMPLEMENTATION_PLAN.md
```

## Tasks

(none yet — a planning pass adds `- [ ]` tasks here before this repo is
dispatched)
